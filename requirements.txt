# K-AHFM-Clinic Python dependencies
#
# 검증된 조합 (전체 파이프라인 실행 기준):
#   Python 3.11 + numpy 1.26.4 + torch (CUDA 12.1 빌드, cu121)
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   numpy는 반드시 1.x — 2.x는 아래 고정 버전들과 ABI가 맞지 않는다.
#
# PyTorch는 별도 설치 (CUDA 12.1 빌드)
# torch-geometric은 setup_env.sh 마지막에 별도 설치
#
# 데모(demo.py)만 돌릴 거라면 이 파일 전체는 필요 없다:
#   pip install torch "numpy<2" scikit-learn

# ===== 한국어 NLP =====
transformers==4.41.2
tokenizers==0.19.1
sentencepiece==0.2.0
huggingface-hub==0.23.4
accelerate==0.31.0

# ===== 데이터 처리 =====
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0

# ===== 시각화 =====
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# ===== 진행 표시 / 로깅 =====
tqdm==4.66.4
loguru==0.7.2
rich==13.7.1

# ===== 설정 관리 =====
pyyaml==6.0.1
hydra-core==1.3.2
omegaconf==2.3.0

# ===== 실험 추적 (옵션) =====
tensorboard==2.16.2
# wandb==0.17.0  # 필요시 주석 해제

# ===== 한국어 처리 보조 -----
konlpy==0.6.0       # 필요시 KoNLPy (보조)
soynlp==0.0.493     # 한국어 분석 보조

# ===== 노트북 / 디버깅 =====
ipython==8.25.0
jupyter==1.0.0
ipywidgets==8.1.3
